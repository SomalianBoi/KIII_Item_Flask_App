apiVersion: v1
kind: Service
metadata:
  name: flaskapp-service
  namespace: flaskapp
spec:
  selector:
    app: flaskapp
  ports:
    - protocol: TCP
      port: 5000        
      targetPort: 5000  

